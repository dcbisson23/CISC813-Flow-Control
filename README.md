TLDR: 
- pump_domain.rddl is the domain file,
- any instance<num>.rddl files are problem instances,
- rddl_loader.py simulates the domain and problem in RDDLGym, and trains a model (either online with replanning or a deep reactive policy) to regulate the system.
